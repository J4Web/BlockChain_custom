"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.capitalizeFirstLetter = exports.camelCase = exports.firstParam = exports.getQueryParam = void 0;
function getQueryParam(url, name) {
    if (!url || !name)
        return null;
    const q = url.match(new RegExp("[?&]" + name + "=([^&#]*)"));
    return q && q[1];
}
exports.getQueryParam = getQueryParam;
const firstParam = (param) => {
    if (!param)
        return "";
    return typeof param === "string" ? param : param[0] || "";
};
exports.firstParam = firstParam;
const camelCase = (str) => {
    return str
        .split(" ")
        .map((x) => x.charAt(0).toUpperCase() + x.slice(1))
        .join("");
};
exports.camelCase = camelCase;
const capitalizeFirstLetter = (value) => {
    return value[0] ? value[0].toUpperCase() + value.slice(1) : "";
};
exports.capitalizeFirstLetter = capitalizeFirstLetter;
//# sourceMappingURL=params.js.map