import { jsx as _jsx } from "@emotion/react/jsx-runtime";
import { formatName } from '@cardinal/namespaces';
import styled from '@emotion/styled';
export const TwitterHandleNFT = ({ handle, dev, cluster, }) => {
    return (_jsx(Outer, { children: _jsx(StyledImg, { alt: formatName('twitter', handle), src: `https://${dev ? 'dev-nft' : 'nft'}.cardinal.so/img/?text=${formatName('twitter', handle)}${cluster && `&cluster=${cluster}`}` }) }));
};
const StyledImg = styled.img `
  border-radius: 10px;
  height: 120px;
  width: 120px;
  box-shadow: 0 10px 16px 0 rgb(0 0 0 / 20%), 0 6px 20px 0 rgb(0 0 0 / 10%);
`;
const Outer = styled.div ``;
//# sourceMappingURL=TwitterHandleNFT.js.map