import { __awaiter } from "tslib";
import { findNamespaceId, getReverseEntry } from '@cardinal/namespaces';
import { useQuery } from 'react-query';
export const useReverseEntry = (connection, namespaceName, pubkey) => {
    return useQuery(['useReverseEntry', namespaceName, pubkey === null || pubkey === void 0 ? void 0 : pubkey.toString()], () => __awaiter(void 0, void 0, void 0, function* () {
        if (!pubkey || !connection)
            return;
        const [namespaceId] = yield findNamespaceId(namespaceName);
        const reverseEntry = yield getReverseEntry(connection, pubkey, namespaceId);
        return reverseEntry || undefined;
    }), { refetchOnMount: false, refetchOnWindowFocus: false });
};
//# sourceMappingURL=useReverseEntry.js.map