import { AnchorProvider, Program } from "@project-serum/anchor";
import { SCANNER_ADDRESS, SCANNER_IDL } from "./constants";
export const scan = (connection, wallet, additionalSigner) => {
    const provider = new AnchorProvider(connection, wallet, {});
    const scannerProgram = new Program(SCANNER_IDL, SCANNER_ADDRESS, provider);
    return scannerProgram.instruction.scan({
        accounts: {
            user: wallet.publicKey,
            scanner: additionalSigner,
        },
    });
};
//# sourceMappingURL=instruction.js.map