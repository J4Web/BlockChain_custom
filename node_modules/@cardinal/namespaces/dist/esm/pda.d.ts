import { PublicKey } from "@solana/web3.js";
/**
 * Finds the namespace id.
 * @returns
 */
export declare const findNamespaceId: (namespaceName: string) => Promise<[PublicKey, number]>;
/**
 * Finds the entry id in a given namespace.
 * @returns
 */
export declare const findNameEntryId: (namespaceId: PublicKey, entryName: string) => Promise<[PublicKey, number]>;
/**
 * Finds the claim request ID for a given namespace and name.
 * @returns
 */
export declare const findClaimRequestId: (namespaceId: PublicKey, entryName: string, requestor: PublicKey) => Promise<[PublicKey, number]>;
/**
 * @Deprecated
 * Finds the deprecated reverse entry ID for a given publickey.
 * @returns
 */
export declare const findDeprecatedReverseEntryId: (pubkey: PublicKey) => Promise<[PublicKey, number]>;
/**
 * Finds the reverse entry ID for a given publickey.
 * @returns
 */
export declare const findReverseEntryId: (namespace: PublicKey, pubkey: PublicKey) => Promise<[PublicKey, number]>;
/**
 * Finds the namespace id.
 * @returns
 */
export declare const findGlobalContextId: () => Promise<[PublicKey, number]>;
//# sourceMappingURL=pda.d.ts.map