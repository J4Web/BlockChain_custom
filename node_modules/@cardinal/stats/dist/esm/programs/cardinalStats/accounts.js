import { __awaiter } from "tslib";
import { AnchorProvider, Program } from "@project-serum/anchor";
import { STATS_ADDRESS, STATS_IDL } from "./constants";
import { findStatsEntryId } from "./pda";
export const getStatsEntry = (connection, name) => __awaiter(void 0, void 0, void 0, function* () {
    // eslint-disable-next-line @typescript-eslint/ban-ts-comment
    // @ts-ignore
    const provider = new AnchorProvider(connection, null, {});
    const statsProgram = new Program(STATS_IDL, STATS_ADDRESS, provider);
    const [statsEntryId] = yield findStatsEntryId(name);
    const parsed = yield statsProgram.account.statsEntry.fetch(statsEntryId);
    return {
        parsed,
        pubkey: statsEntryId,
    };
});
//# sourceMappingURL=accounts.js.map